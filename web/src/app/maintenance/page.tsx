'use client';

import { AppShell } from '@/components/AppShell';

export default function MaintenancePage() {
  return (
    <AppShell title="维修与计量">
      <div className="bg-white/60 backdrop-blur-[20px] rounded-3xl p-6">
        <p>维修与计量页面内容</p>
        {/* 这里可以添加维修与计量的具体功能 */}
      </div>
    </AppShell>
  );
}